import { StyleSheet } from 'react-native'

export default StyleSheet.create({
  container: {
    marginTop: 20,
    paddingHorizontal: 8,
  },
  leftButton: {
    marginLeft: 4,
    paddingHorizontal: 8,
  }
})
