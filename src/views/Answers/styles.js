import { StyleSheet } from 'react-native'

export default StyleSheet.create({
  container: {
    marginTop: 20,
    paddingHorizontal: 8,
  },
  rightButton: {
    marginRight: 8,
    paddingHorizontal: 12,
  }
})
