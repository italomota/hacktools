import { StyleSheet, Dimensions } from 'react-native'

export default StyleSheet.create({
  container: {
    backgroundColor: '#167bf7',
    borderRadius: 4,
    paddingVertical: 8,
    alignItems: 'center',
    width: '100%',
  },
  title: {
    color: '#fff',
    fontWeight: 'bold',
  },
})
