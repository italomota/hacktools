import { StyleSheet } from 'react-native'

export default StyleSheet.create({
  textInput: {
    height: 40,
    width: '100%',
    borderWidth: 1,
    borderRadius: 4,
    borderColor: '#bbb',
    paddingLeft: 16,
  },
})
